# Optional AI provider keys used by the backend Python assistant.
# Copy this file to .env and fill in values before running docker compose.
GEMINI_API_KEY=
GITHUB_TOKEN=

# Used by the backend to sign dashboard access tokens.
# Change this before deploying beyond local development.
AUTH_TOKEN_SECRET=redhawk-dev-secret-change-me
